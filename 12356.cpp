#include <bits/stdc++.h>

using namespace std;

int arr[100000];

int main()
{
  int a, b;
  while (scanf("%d %d", &a, &b) == 2)
  {

    for (int i = 0; i <= a; i++)
      arr[i] = 0;
    scanf("%d %d", &a, &b);
  }
  return 0;
}